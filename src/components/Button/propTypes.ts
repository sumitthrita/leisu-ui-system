export enum Variant {
  Primary = "primary",
  Secondary = "secondary",
  Ghost = "ghost",
}

export enum Size {
  Small = "small",
  Medium = "medium",
  Large = "large",
}

export enum Width {
  Auto = "auto",
  Full = "full",
}

export enum State {
  Default = "default",
  Hover = "hover",
  Focus = "focus",
  Active = "active",
  Disabled = "disabled",
}
